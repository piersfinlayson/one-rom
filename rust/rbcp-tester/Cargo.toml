[package]
name = "onerom-rbcp-tester"
version = "0.1.0"
edition = "2024"
# Workspace-internal: needs CONFIG/BOARD and a firmware C tree to build.
publish = false
authors = ["Piers Finlayson <piers@piers.rocks>"]
license = "MIT"
description = "Runs the host-control plugin against the emulated firmware, over emulated ROM bus cycles"
repository = "https://github.com/piersfinlayson/one-rom"
homepage = "https://onerom.org"
keywords = ["retro", "rom", "commodore", "embedded"]
categories = ["embedded", "hardware-support"]

[[bin]]
name = "rbcp-tester"
path = "src/main.rs"

[dependencies]
onerom-config = { workspace = true }
onerom-fw-emulator = { workspace = true }
onerom-fw-tester = { workspace = true }
onerom-gen = { workspace = true }
onerom-plugin-tester = { workspace = true }

serde_json = "1"
log = "0.4"
env_logger = "0.11"

[lints]
workspace = true
