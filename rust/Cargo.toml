# Workspace configuration for the Rust elements of the One ROM project
[workspace]
members = [
    "app",
    "cli",
    "config",
    "database",
    "fw",
    "fw-config-gen",
    "fw-emulator",
    "fw-tester",
    "fw-parser",
    "gen",
    "lab",
    "metadata",
    "protocol",
    "schema-gen",
    "studio",
]
default-members = [
    "app",
    "cli",
    "config",
    "database",
    "fw",
    "fw-config-gen",
    "fw-parser",
    "gen",
    "metadata",
    "protocol",
    "schema-gen",
    "studio",
]
resolver = "2"

[workspace.dependencies]
onerom-app = { path="app", version="0.1.3" }
onerom-config = { path="config", version="0.5.3" }
onerom-database = { path="database", version="0.1.1" }
onerom-fw = { path="fw", version="0.1.25" }
onerom-fw-emulator = { path="fw-emulator", version="0.1.0" }
onerom-fw-parser = { path="fw-parser", version="0.7.3" }
onerom-gen = { path="gen", version="0.6.3" }
onerom-metadata = { path="metadata", version="0.1.3" }
onerom-protocol = { path="protocol", version="0.1.0" }

[profile.release]
opt-level = 3
#lto = true
codegen-units = 1
strip = true
panic = "abort"
